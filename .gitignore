# Optional: keeps a local work-state directory from showing up as
# untracked noise. Not a hard block — if you want to commit it, remove
# the matching line or use `git add -f`.
work_state/
work-state/
state/
daily/
.claude/settings.local.json
